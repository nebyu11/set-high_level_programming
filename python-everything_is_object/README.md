# Python - Everything is object

This directory contains tasks exploring the Python object model: mutability vs. immutability, variables, references, identity, equality, interning, and how arguments are passed to functions.
