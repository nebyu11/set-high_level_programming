# Python - More Classes and Objects

This directory contains advanced tasks focusing on classes and objects in Python. It includes implementations of the `Rectangle` class using properties, custom string representations, instance counts, static/class methods, and a backtracking solver for the N Queens puzzle.
