# Python - Classes and Objects

This directory contains tasks relating to object-oriented programming (OOP) in Python. It includes implementations of the `Square` class, including instantiation, encapsulation, type/value validation of fields, getters/setters, instance methods, and comparisons.
